/**
 * The different location types supported by X Jobs search.
 *
 * @public
 */
export enum JobLocationType {
	HYBRID = 'hybrid',
	ONSITE = 'onsite',
	REMOTE = 'remote',
}

/**
 * The different seniority levels supported by X Jobs search.
 *
 * @public
 */
export enum JobSeniorityLevel {
	ENTRY_LEVEL = 'entry_level',
	EXECUTIVE = 'executive',
	INTERN = 'intern',
	JUNIOR = 'junior',
	LEAD = 'lead',
	MANAGER = 'manager',
	MID_LEVEL = 'mid_level',
	SENIOR = 'senior',
}

/**
 * The different employment types supported by X Jobs search.
 *
 * @public
 */
export enum JobEmploymentType {
	CONTRACT_TO_HIRE = 'contract_to_hire',
	FULL_TIME = 'full_time',
	FULL_TIME_CONTRACT = 'full_time_contract',
	PART_TIME = 'part_time',
}
