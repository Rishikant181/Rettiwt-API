/**
 * The details of a single X Job location suggestion.
 *
 * @public
 */
export interface IJobLocation {
	/** The location id to use with X Jobs search. */
	id: string;

	/** The display name of the location. */
	name: string;
}
